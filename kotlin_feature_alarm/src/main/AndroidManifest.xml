<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application>
        <receiver
            android:name=".domain.receiver.FeatureAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="co.id.fadlurahmanfdev.kotlin_feature_alarm.ACTION_SET_ALARM" />
            </intent-filter>
        </receiver>

        <service
            android:name=".domain.service.FeatureAlarmService"
            android:exported="false">
            <intent-filter>
                <action android:name="co.id.fadlurahmanfdev.kotlin_feature_alarm.ACTION_PLAY_ALARM" />
            </intent-filter>
        </service>
    </application>
</manifest>